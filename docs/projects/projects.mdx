import React from 'react';
import { CodeButton } from '../../src/components/CodeButton';
import { DemoButton } from '../../src/components/DemoButton'; 

export const Projects = ({ title, description, imgSrc }) => {
    return (
        <section style={{
            display: "flex", 
            gap: "2rem", 
            flexWrap: "wrap",
            alignItems: "center",
            padding: "2rem",
            borderRadius: "1rem",
            boxShadow: "0 4px 8px rgba(0, 0, 0, 0.3)"
        }}>
            <div style={{ flex: "1" }}>
                <img src={imgSrc} alt="Project Thumbnail" width="100%" style={{
                    height: "auto",
                    border: "2px solid #d3d3d3",
                    borderRadius: "1rem",
                    transition: "transform 0.3s ease",
                    maxWidth: "400px"
                }} 
                onMouseEnter={e => e.currentTarget.style.transform = "scale(1.05)"}
                onMouseLeave={e => e.currentTarget.style.transform = "scale(1)"}
                />
            </div>
            <div style={{ flex: "2", textAlign: "left" }}>
                <h2 style={{ fontSize: "2rem", fontWeight: "bold", marginBottom: "0.5rem" }}>
                    {title}
                </h2>
                <p style={{ fontSize: "1.1rem", lineHeight: "1.6", marginBottom: "1rem" }}>
                    {description}
                </p>
                <div style={{ display: "flex", gap: "1rem" }}>
                    <CodeButton codeLink="https://github.com/nixoletas/intranet-govbr" />
                    <DemoButton liveLink="/" />
                </div>
            </div>
        </section>
    );
};

<Projects 
    imgSrc="/img/intranet-govbr.png" 
    title="GovBR Intranet" 
    description="Production Intranet for my battalion made with (Express | Angular | Strapi)"
/>
